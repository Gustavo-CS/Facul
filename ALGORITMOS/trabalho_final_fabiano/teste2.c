
// #include <stdio.h>

// int main()
// {
//     // char vetor[][]; // Um exemplo de vetor de tamanho 5
//     // int a[3] = {1, 2, 3};
//     // int *p = a;
//     // printf("%zu\n", sizeof a); // prints size of array
//     // printf("%zu\n", sizeof p);

//         // Verificando se o vetor está vazio
//         // if (len == 0)
//         // {
//         //     printf("O vetor esta vazio.\n");
//         // }
//         // else
//         // {
//         //     printf("essa porra nao funcina %d\n", sizeof(vetor));
//         // }

//         // for (int j = 0; sizeof(vetor) > j; j++)
//         // {
//         //     int len = sizeof(vetor[0]) / sizeof(vetor[0][0]);
//         //     if (vetor[0][j] != int){
//         //         printf("ta vazio \n \n");
//         //     }
//         //     printf("essa porra nao funcina %d  %d\n", vetor[0][j], j);

//         // }

//         return 0;
// }

// #include <stdio.h>

// int main() {
//     int vetor[10];
//     vetor[0] = 5;
//     size_t tamanho = sizeof(vetor) / sizeof(vetor[0]);

//     printf("O tamanho do vetor e: %zu\n", tamanho);

//     return 0;
// }

#include <stdio.h>

// void funcao(int vetor[], size_t tamanho) {
//     printf("O tamanho do vetor e: %zu\n", tamanho);
//     // Resto da lógica da função
// }

int main()
{
    // int vetor[10];
    // size_t tamanho = sizeof(vetor) / sizeof(vetor[0]);

    // funcao(vetor, tamanho);
    int i = 0;
    while (i != 10)
    {
        int carro = 0;
        if (carro > 0)
        {
            int b;
        }
        else
        {
            carro = 0;
        }
        printf("%d \n", carro);
        carro++;
        i++;
    }

    return 0;
}
